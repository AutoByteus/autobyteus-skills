interface:
  display_name: "Computer Use Playbook"
  short_description: "DOM-first automation across browser and desktop"
  default_prompt: "Use $computer-use-playbook for browser-first automation with explicit tab_id per browser call, then escalate to vision/native only when needed, with speak-based user handoff for login/CAPTCHA gates."
