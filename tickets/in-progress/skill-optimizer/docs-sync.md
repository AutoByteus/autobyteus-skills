# Docs Sync

No long-lived repository documentation update is needed. `README.md` is an infographic-oriented overview and does not enumerate every skill; adding a non-illustrated entry would create an inconsistent catalog rather than document a changed repository convention.
